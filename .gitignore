# ── Dependencies ──
node_modules/
client/node_modules/
server/node_modules/

# ── Environment variables (contains secrets — never commit) ──
.env
.env.local
.env.*.local
client/.env
server/.env

# ── Build output ──
client/dist/
client/build/
server/dist/

# ── Logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# ── OS files ──
.DS_Store
Thumbs.db

# ── Editor/IDE ──
.vscode/
.idea/
*.swp
*.swo

# ── Vite ──
.vite/

# ── Puppeteer (downloaded Chromium binary, do not commit) ──
.cache/
.local-chromium/

# ── Misc ──
*.tmp
*.temp
.env.example.local
